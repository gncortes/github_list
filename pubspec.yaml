name: github_list
description: Aplicativo para listar os 100 primeiros repositórios do GitHub.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  build_runner: ^1.10.12
  dio: ^3.0.10
  either_dart: ^0.0.5
  flutter_mobx: ^1.1.0+2
  mobx: ^1.2.1+4
  mobx_codegen: ^1.1.2
  url_launcher: ^5.7.10

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
